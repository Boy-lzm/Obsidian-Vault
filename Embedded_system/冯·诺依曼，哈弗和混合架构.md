《嵌入式C语言自我修养，从芯片编译器到操作系统》
1. 冯·诺依曼架构
![[Pasted image 20260324232155.png|298]]
特点是指令和数据混合存储，存储在同一块存储器的不同位置上。一般是在外存储器上，程序运行时加载到内存储器。
结构简单，x86，ARM7等处理器采用该架构

2. 哈弗架构
![[Pasted image 20260324232623.png|332]]
指令和数据分开存储，分别放到程序存储器和数据存储器。每个存储器都独立编址，独立访问，而且指令和数据可以在一个时钟周期内并行访问

3. 混合架构
CPU工作频率越来越高，和RAM产生带宽问题：：CPU频率可达GHz，而RAM一般几百兆赫兹。CPU和SRAM之间传输数据要经过找地址，取数据，配置，等待，输出数据等多个时钟周期，影响效率。从而引入cache机制：指令Cache和数据Cache。

现代ARM SoC芯片采用架构如下：
	1. SoC芯片内部Cache采用哈弗架构，集成指令cache和数据cache。
	SRAM一次性缓存一批数据到cacha中，CPU下一次去指令和数据时先看是否已经在cache中了，如果没有缓存命中。再到内存中读取。
	2. SoC外部依旧采用冯·诺依架构，因此称为混合架构
![[Pasted image 20260324233517.png|371]]

引入流水线后，虽然每一条指令的执行流程和时间不变，还是需
要3个时钟周期，但是从整条流水线的输出来看，差不多平均每个时钟
周期就能执行一条指令。原来执行一条指令需要3个时钟周期，引入流
水线后平均只需要1个时钟周期，CPU性能提升了不少。

![[Pasted image 20260324235706.png|530]]
![[Pasted image 20260324235741.png]]


x86和ARM架构的区别
1. 指令集设计（最本质区别）
x86 复杂指令集计算，CISC,  采用复杂指令集，指令长度可变，功能强大。一条指令可能完成多个底层操作。
    
- **优势**：在复杂任务处理上，前端译码虽复杂，但配合庞大的乱序执行引擎，能实现极高的单核性能和极低延迟。
    
- **劣势**：硬件电路复杂，解码单元功耗高，晶体管数量多，因此**功耗相对较高**。

1. **ARM（精简指令集计算，RISC）**
- 采用精简指令集，指令长度固定（如32位或16位），每条指令只完成一个简单操作。
    
- **优势**：硬件逻辑相对简单，解码功耗极低，非常适合对功耗和散热敏感的设备。
    
- **劣势**：早期在极端高性能场景（如超算顶级节点）表现稍弱，但近年来随着高性能设计（如Apple M系列、NVIDIA Grace）的推进，这一差距正在迅速缩小。


RISC-V
RISC-V架构和ARM一样，都属于RISC指令集，RISC-V属于RISC的第五个版本
