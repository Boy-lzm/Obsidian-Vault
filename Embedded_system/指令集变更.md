1. ARM的32位指令集：早期的ARM处理器（ARM7TDMI之前）支持名为ARM的32位指令集
	 指令集功能强大，支持多数指令的条件执行，并可以提供良好的性能，但是与8位和16位指令相比，其需要更多存储空间。
2. Thumb指令：1995年，ARM7TDMI可以允许新的16位指令集的运行状态，称为thumb。ARM7TDMI可以运行在ARM状态也可以运行在thumb状态，处理器可以通过软件控制ARM和thumb状态切换。
	 尽管Thumb指令可以实现ARM指令的大多数功能，他还是有局限性，如操作时可选的寄存器，可用的寻址模式，以及用于数据或地址的立即数范围较小等。
	 