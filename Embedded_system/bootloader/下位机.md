加密过程
	*srec文件中前面的数据被写到低地址，后面的数据被写到高地址*
1. 对srec文件的数据部分进行处理，按照小段模式重新生成，比如原来的00400020转为20004000，形成一个完整的srec字符串。
2. 将srec字符串按ISO-8859-1解码为码字节类型
3. 用SHA.new对srec字节类型进行计算，输出一个256长度的hash值
4. 将pem文件中的密钥使用RSA生成一个密钥对象，供后续密码学使用
5. 使用pkcs1_15签名方案，用RSA私钥对哈希值进行签名